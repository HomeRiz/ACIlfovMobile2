// Test minimal (inlocuieste testul auto-generat care folosea vechiul MyApp).
// Testul real de securitate este in allowlist_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder smoke test', () {
    expect(1 + 1, 2);
  });
}
